import React, {Component} from 'react';

class Header extends Component {
    render() {
        return (
            <header>
                <a href="/">Assistant</a>
            </header>
        );
    }
}

export default Header;